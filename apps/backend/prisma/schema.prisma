generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model PokemonCard {
  id        Int      @id @default(autoincrement())
  name      String
  createdAt DateTime @default(now())
}