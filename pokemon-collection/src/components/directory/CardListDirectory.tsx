// components
import Card from '../card/Card';

const CardListDirectory = ({ title }: any) => {
  return (
    <div>
      <Card title={title} />
    </div>
  );
};

export default CardListDirectory;
